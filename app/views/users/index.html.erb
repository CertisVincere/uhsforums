<h1>Users</h1>

<%= will_paginate %>

<ul class = "users">
  <%= render @users %>
</ul>

<%= will_paginate %>

<%= will_paginate %>

<ul class="groups">
  <%= render @groups %>
</ul>

<%= will_paginate %>
